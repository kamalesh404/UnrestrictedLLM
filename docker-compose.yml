version: '3.8'

services:
  unrestrictedllm:
    build: .
    command: serve --port 8080
    ports:
      - "8080:8080"
    volumes:
      - ~/.unrestrictedllm:/root/.unrestrictedllm
    restart: unless-stopped
